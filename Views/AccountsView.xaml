<UserControl x:Class="FinancialSystem.Views.AccountsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <DataGrid Grid.Column="0" ItemsSource="{Binding Accounts}" 
                  SelectedItem="{Binding SelectedAccount}" 
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" Margin="0,0,15,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Номер рахунку" Binding="{Binding AccountNumber}" Width="*"/>
                <DataGridTextColumn Header="Баланс" Binding="{Binding Balance}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Column="1" Background="#FFF5F5F5" Margin="0" UseLayoutRounding="True">
            <Border Background="#FFE0E0E0" Padding="10">
                <TextBlock Text="Операції з рахунком" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center"/>
            </Border>

            <StackPanel Margin="15">
                <TextBlock Text="Введіть суму:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding TransactionAmount, UpdateSourceTrigger=PropertyChanged}" 
                         Height="25" Margin="0,0,0,15" Padding="2"/>

                <Button Content="Поповнити (Deposit)" Command="{Binding DepositCommand}" 
                        Height="35" Margin="0,0,0,10" Background="#FFD4EDDA"/>

                <Button Content="Зняти (Withdraw)" Command="{Binding WithdrawCommand}" 
                        Height="35" Margin="0,0,0,10" Background="#FFF8D7DA"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
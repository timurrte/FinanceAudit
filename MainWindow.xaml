<Window x:Class="ExpenseTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Expense Tracker" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" Background="#F0F2F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="80"/>
        </Grid.RowDefinitions>

        <Border Background="White" Padding="20,15">
            <TextBlock Text="My Financial Dashboard" FontSize="22" FontWeight="SemiBold" Foreground="#333"/>
        </Border>

        <Border Grid.Row="1" Margin="20" Background="White" CornerRadius="10" Padding="15">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Expenses Over Time" FontSize="16" FontWeight="Bold" Foreground="#555" Margin="0,0,0,10"/>

                <lvc:CartesianChart Grid.Row="1" Series="{Binding ExpenseSeries}" LegendLocation="None">
                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Title="Month" Labels="{Binding Labels}"/>
                    </lvc:CartesianChart.AxisX>
                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Amount" LabelFormatter="{Binding YFormatter}"/>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>

        <Border Grid.Row="2" Margin="20,0,20,0" Background="White" CornerRadius="10" Padding="0">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.05"/>
            </Border.Effect>
            <DataGrid ItemsSource="{Binding Expenses}" 
                      AutoGenerateColumns="False" 
                      CanUserAddRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      Background="Transparent"
                      BorderThickness="0"
                      RowHeight="35">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding DisplayDate}" Width="150"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="100" FontWeight="Bold"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Button Grid.Row="3" Width="60" Height="60" 
                VerticalAlignment="Top" HorizontalAlignment="Center" 
                Margin="0,-30,0,0"
                Click="AddExpense_Click"
                Background="#007ACC" BorderThickness="0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="30"/>
                </Style>
            </Button.Resources>
            <Button.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.3" Color="#007ACC"/>
            </Button.Effect>
            <TextBlock Text="+" FontSize="36" Foreground="White" Margin="0,0,0,4"/>
        </Button>

    </Grid>
</Window>